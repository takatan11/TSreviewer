# Reviewer.js 作業メモ（オフライン用）

最終整理日: 2026-08-09

## ⚠️ 機内（オフライン）での注意
- **Supabase は使えない** → 検索・サジェスト・カード表示・登録は実行するとエラーになる。
  DBに繋がる部分の「動作確認」は機内では不可。**コードを書く・UIを作る**のは可能。
- `npm install` も不可。必要なパッケージは搭乗前に入れておくこと。
- `npm run dev` でサーバー自体は起動する（DBに触れないページなら表示できる）。

---

## ✅ 完成している部分
- ホーム `/`：検索欄・学部・学科・検索ボタンを**1つのフォームに統合済み**。
- 絞り込み検索：授業名=部分一致(`ilike`)、学部・学科=完全一致(`eq`)。DB側で絞り込み。
- サジェスト `/api/suggest`：入力に応じて候補表示、クリックで入力欄に反映。
- 検索結果カード：検索したときだけ表示。カードは `/subject/授業名` へリンク（encodeURIComponent済み）。
- レビュー登録 `/registration`：GET（フォーム表示）と POST（DB保存）が動作。
- `id`/`for` の対応、曜日プルダウンの `<select>` 追加など、フォームの体裁を修正済み。

---

## 🔨 作成中
### `/subject/:name`（授業ごとの詳細ページ）
- 現状は `<h1>ニャーーーん</h1>` のプレースホルダーのみ。
- やること：
  1. `const name = c.req.param('name')` で授業名を取得。
  2. `supabase.from('subject').select('*').eq('class_name', name).single()` で1件取得
     （class_name は一意にしてあるので `.single()` が使える）。
  3. 授業の詳細＋その授業のレビュー一覧を表示。
  4. コメント（レビュー）入力欄もこのページ内に移す（※自分のメモより）。

### `/new-class`（新しい授業の登録フォーム）
- 画面（GET）は作成中。各項目のセレクトは並んだが **`name` 属性が未設定のものがある**
  （時限=period / 学部=faculty / 学科=depart など）。`name` が無いと送信時に値が届かない。
- **`app.post('/new-class')` が未作成** → 実際に授業を登録する処理がまだ無い。

---

## ⛔ 未着手・未解決
1. **`/reviews` ルートが無い**：ホーム68行のリンク「すべての授業を見に行く」は今 404。
   `app.get('/reviews')` を定義するか、リンクを消す。
2. **⚠️ レビューと授業の紐付けが未設計（最重要）**：
   `review` テーブルに保存しているのは `comment` と `score` だけ。
   「どの授業へのレビューか」を持っていないので、`/subject/:name` でレビュー一覧を出せない。
   → `review` テーブルに授業を指す列（例 `subject_id` か `class_name`）を追加し、
     登録フォームからその値も送って保存する設計が必要。
3. **登録フォームの講師名(`instructor`)が保存されていない**：
   入力欄はあるが、POST の `.insert({...})` に含まれていない（comment と score のみ）。

---

## 進める順番のおすすめ
1. まず「レビューと授業の紐付け」を決める（上記2）。これが決まらないと詳細ページも作れない。
2. その上で `/subject/:name` の中身を実装。
3. `/new-class` の各セレクトに `name` を付け、`app.post('/new-class')` を作る。
4. `/reviews` を作る or リンク削除。

---

## メモ：属性の役割（混同しやすい点）
- `class` … CSS用。DBと無関係。何でもよい。
- `id` / `for` … ページ内でラベルと入力欄を結ぶ。1対1で固有に。
- `name` … 送信キー。サーバーで `body.○○` として受け取る。DB列名と揃えると楽（必須ではない）。
- **DBのカラム名を実際に書くのは Supabase のメソッド引数だけ**
  （`.select('...')` / `.eq('列名', 値)` / `.ilike('列名', ...)` / `.insert({ 列名: 値 })`）。
- 学科の列名は `depart`（`department` ではない）。
